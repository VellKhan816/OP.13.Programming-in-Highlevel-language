import sys
from PyQt6.QtWidgets import *
from PyQt6.QtCore import Qt

class ProductCard(QWidget):
    def __init__(self):
        super().__init__()
        self.setFixedHeight(280)
        self.setStyleSheet("font-weight: bold;")  # общий стиль для всех надписей
        self.setup_ui()

    def setup_ui(self):
        layout = QHBoxLayout()
        layout.setContentsMargins(5, 5, 5, 5)
        layout.setSpacing(10)

        # --- Левая колонка: "Фото" ---
        photo = QLabel("Фото")
        photo.setAlignment(Qt.AlignmentFlag.AlignCenter)
        photo.setFixedSize(200, 200)
        photo.setStyleSheet("""
            border: 2px solid black;
            padding: 10px;
            font-size: 14px;
            background-color: #f0f0f0;
        """)

        # --- Центральная колонка: Информация ---
        info = QLabel(
            "Электроника | Умные часы X1\n"
            "Описание товара: Сердечный ритм, GPS, водонепроницаемость\n"
            "Производитель: TechCorp\n"
            "Поставщик: ГлобалТрейд\n"
            "Цена: 9 990 ₽\n"
            "Единица измерения: шт.\n"
            "Количество на складе: 15"
        )
        info.setFrameShape(QFrame.Shape.Box)
        info.setFrameShadow(QFrame.Shadow.Plain)
        info.setLineWidth(2)
        info.setContentsMargins(10, 10, 10, 10)
        info.setMinimumWidth(400)  # чтобы не сжималось

        # --- Правая колонка: Скидка ---
        discount = QLabel("Персональная\nскидка: 15%")
        discount.setAlignment(Qt.AlignmentFlag.AlignCenter)
        discount.setFixedSize(120, 200)
        discount.setStyleSheet("""
            border: 2px solid black;
            padding: 10px;
            font-size: 14px;
            text-align: center;
        """)

        # Добавляем всё в основной макет
        layout.addWidget(photo)
        layout.addWidget(info)
        layout.addWidget(discount)

        self.setLayout(layout)


if __name__ == "__main__":
    app = QApplication(sys.argv)
    window = ProductCard()
    window.setWindowTitle("Карточка товара")
    window.resize(800, 300)
    window.show()
    sys.exit(app.exec())
